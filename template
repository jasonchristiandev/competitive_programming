#include <bits/stdc++.h>
#define int long long
#define float double
#define lfloat long double
using vb = std::vector<bool>;
using vvb = std::vector<vb>;
using vi = std::vector<int>;
using vvi = std::vector<vi>;
using vvvi = std::vector<vvi>;
using vf = std::vector<float>;
using vvf = std::vector<vf>;
using vvvf = std::vector<vvf>;
using mii = std::map<int, int>;
using mif = std::map<int, float>;
using pii = std::pair<int, int>;
using pff = std::pair<float, float>;
using vpii = std::vector<pii>;
using vvpii = std::vector<vpii>;
using vvvpii = std::vector<vvpii>;
using vpff = std::vector<pff>;
using vvpff = std::vector<vpff>;
using vvvpff = std::vector<vvpff>;
const char endl = '\n';
const int INF = 1e18;
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
#define len(x) (int) (x).size()
#ifdef LOCAL
#define debug(x) std::cout << "( " << #x << " : " << universal_to_string(x) << " )" << endl
#define info(x) std::cout << "( " << universal_to_string(x) << " )" << endl
#define here std::cout << "HERE" << endl
template <typename T>
std::string universal_to_string(const T &value);
std::string universal_to_string(const std::string &s) {
	return "\"" + s + "\"";
}
std::string universal_to_string(const char *s) {
	return "\"" + std::string(s) + "\"";
}
template <std::size_t N>
std::string universal_to_string(const char (&arr)[N]) {
	return "\"" + std::string(arr) + "\"";
}
template <typename T>
	requires std::is_arithmetic_v<T>
std::string universal_to_string(const T &value) {
	return std::to_string(value);
}
template <typename T1, typename T2>
std::string universal_to_string(const std::pair<T1, T2> &p) {
	return "(" + universal_to_string(p.first) + ", " + universal_to_string(p.second) + ")";
}
template <typename T>
concept IsIterable = requires(T t) {
	{ t.begin() } -> std::input_or_output_iterator;
	{ t.end() } -> std::input_or_output_iterator;
} && !std::is_same_v<T, std::string>;
template <IsIterable T>
std::string universal_to_string(const T &container) {
	std::string result = "[";
	bool first = true;
	for (const auto &item : container) {
		if (!first) result += ", ";
		result += universal_to_string(item);
		first = false;
	}
	result += "]";
	return result;
}
#else
#define debug(x)
#define info(x)
#define here
#endif

void solve() {

}

int32_t main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	int t = 1;
	// std::cin >> t;
	while (t--) {
		solve();
	}

	return 0;
}